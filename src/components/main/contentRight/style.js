import styled from "styled-components";

export const ImageContainer = styled.div`
	position: absolute;
	right: 0px;
	top: 65px;

	max-width: 50%;
	width: 100%;
	.profile {
		position: relative;
	}
	.ticket {
		z-index: 2;
		position: absolute;
		right: 0px;
		bottom: -70px;
	}
`;
